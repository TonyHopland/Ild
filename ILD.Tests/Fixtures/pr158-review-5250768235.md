### 🟡 Changes recommended

Unresolved attachment lifecycle and submission-race findings include a critical in-flight upload discard path.

_Get a fresh assessment by requesting another Copilot review._

<details>
<summary>Pull request overview</summary>

Adds end-to-end work-item attachment support for creation, editing, feedback responses, and overview management.

**Changes:**

- Adds attachment types, utilities, limits, uploads, downloads, and deletion.
- Adds shared picker, drag/drop/paste staging with retries and validation.
- Adds listing controls, feedback integration, tests, styling, and changelog updates.

</details>

<details>
<summary>File summaries</summary>

| File                                                                              | Summary                                 |
| --------------------------------------------------------------------------------- | --------------------------------------- |
| `frontend/src/utils/attachments.ts`                                               | Attachment formatting and note helpers  |
| `frontend/src/utils/attachments.test.ts`                                          | Utility tests                           |
| `frontend/src/types/index.ts`                                                     | Attachment metadata and limits types    |
| `frontend/src/services/auth.ts`                                                   | Attachment service methods              |
| `frontend/src/services/attachments.test.ts`                                       | Attachment API contract tests           |
| `frontend/src/services/api.ts`                                                    | Multipart and blob request support      |
| `frontend/src/components/workitem-v2/WorkItemModalV2.tsx`                         | Staging lifecycle and close handling    |
| `frontend/src/components/workitem-v2/WorkItemModalV2.test.tsx`                    | Modal behavior tests                    |
| `frontend/src/components/workitem-v2/WorkItemModalV2.respondBusy.test.tsx`        | Busy-answer tests                       |
| `frontend/src/components/workitem-v2/WorkItemModalV2.respondAttachments.test.tsx` | Feedback attachment tests               |
| `frontend/src/components/workitem-v2/WorkItemModalV2.attachments.test.tsx`        | Create/edit upload tests                |
| `frontend/src/components/workitem-v2/WorkItemModalV2.attachmentList.test.tsx`     | Attachment list tests                   |
| `frontend/src/components/workitem-v2/workitem-v2.css`                             | Attachment styling                      |
| `frontend/src/components/workitem-v2/useWorkItemDetail.ts`                        | Shared staging and response integration |
| `frontend/src/components/workitem-v2/useWorkItemDetail.respond.test.ts`           | Response concurrency tests              |
| `frontend/src/components/workitem-v2/useWorkItemDetail.attachments.test.ts`       | Staging integration tests               |
| `frontend/src/components/workitem-v2/useAttachmentStaging.ts`                     | Shared staging and retry logic          |
| `frontend/src/components/workitem-v2/useAttachmentStaging.test.ts`                | Staging lifecycle tests                 |
| `frontend/src/components/workitem-v2/panels.tsx`                                  | Feedback and overview wiring            |
| `frontend/src/components/workitem-v2/EditPanel.tsx`                               | Create/edit attachment uploads          |
| `frontend/src/components/workitem-v2/AttachmentPicker.tsx`                        | Picker, drop, paste, and staged rows    |
| `frontend/src/components/workitem-v2/AttachmentPicker.test.tsx`                   | Picker tests                            |
| `frontend/src/components/workitem-v2/AttachmentList.tsx`                          | List, download, and removal controls    |
| `frontend/src/components/FeedbackActions.tsx`                                     | Busy-state answer controls              |
| `CHANGELOG.md`                                                                    | User-facing feature entry               |

</details>

<details>
<summary>Review details</summary>

### Suppressed comments (8)

**frontend/src/components/workitem-v2/AttachmentList.tsx:57**

- Deleting an attachment from the overview does not invalidate its corresponding staged entry, which remains marked `uploaded`. If this is done after a partial answer failure, the next retry reports that stale entry in `storedNames` without uploading it, so the conversation names a file that is no longer on the work item; reconcile staged entries after deletion/refetch.

```
  const remove = (attachment: WorkItemAttachment) =>
    act(attachment, `Failed to remove ${attachment.fileName}.`, async () => {
      await workItemService.deleteAttachment(workItem.id, attachment.id);
      onChanged();
    });
```

**frontend/src/components/workitem-v2/AttachmentPicker.tsx:64**

- An entry marked `uploaded` is kept in the shared staging list indefinitely. If a file lands during a failed answer, the user can switch to Overview and delete it, then retry; `uploadAll` will still report its name in `storedNames` without re-uploading it, so the answer tells the agent about a file that is no longer attached. Reconcile uploaded staged entries with the current attachment list (or invalidate them) before constructing the note.

```
              {/* An uploaded file is on the work item, where the overview's own
                  list removes it. Dropping it from here would only stop the
                  answer's note from naming a file that is there. */}
              {entry.status === "uploaded" ? (
                <span className="wiv2-attach-done">Attached</span>
```

**frontend/src/components/workitem-v2/AttachmentPicker.tsx:36**

- The picker remains usable after `uploadAll` finishes and while the subsequent answer/save request is in flight. A file added in that window is neither uploaded nor included in the note, and the success path then calls `attachments.clear()`, silently discarding it; disable staging for the whole submit lifecycle or retain/re-upload files added after the batch.

```
        <input
          id={inputId}
          type="file"
          multiple
          className="wiv2-attach-input"
```

**frontend/src/components/workitem-v2/WorkItemModalV2.tsx:92**

- The new dirty check makes staged files prompt for confirmation, but the two discard-confirm callbacks below still call only `onClose()` and never `clearStaged()`. Since this staging hook is shared with the feedback pane, confirming discard leaves the files in the hook instead of clearing the act as required; clear the staging state before closing on both discard paths.

```
  const hasUnsavedChanges =
    ((editMode || isCreate) && editDirty) ||
    detail.feedbackInput.trim().length > 0 ||
    detail.attachments.staged.length > 0;
```

**frontend/src/components/workitem-v2/useAttachmentStaging.ts:157**

- The upload response contains the server-created attachment metadata, but it is discarded here and `storedNames` later comes from `entry.file.name`. The WorkItem server normalizes names (for example, trimming whitespace and stripping path segments), so a valid name such as `report.txt` can be stored as `report.txt` while the conversation note names the unnormalized value. Use the returned attachment `fileName` when building `storedNames`.

```
            await workItemService.uploadAttachment(targetWorkItemId, entry.file);
```

**frontend/src/components/workitem-v2/useAttachmentStaging.ts:94**

- If a file is selected before `GET /settings/attachments` resolves, `limits` is `undefined` here and the file is accepted. Once the limit arrives, staged entries are never revalidated, so an oversized file can still be sent by `uploadAll`, violating the client-side no-request guarantee; revalidate pending entries when limits load or defer staging until the limit is known (the exception for a failed limits request should remain).

```
      const maxBytes = limits?.maxBytesPerFile;
      const now = new Date();
      const accepted: File[] = [];
      const refusals: string[] = [];
      for (const file of incoming) {
```

**frontend/src/components/workitem-v2/useWorkItemDetail.ts:607**

- Because the feedback picker remains active while `respondLoading` is true, a file can be staged after `uploadAll` returns but before `submit` resolves (and the same race occurs when the answer started with no staged files). The success path then calls `attachments.clear()` and silently discards that new file without uploading it. Lock staging for the whole response or snapshot/reject additions before clearing.

```
      attachments.clear();
```

**frontend/src/utils/attachments.ts:30**

- `attachedNote` is documented as preserving what the human typed, and the backend records this string verbatim, but `trim()` removes leading/trailing whitespace and newlines whenever attachments are present. This changes the submitted note only for file-bearing answers; preserve the original text and use a separate emptiness check.

```
  const text = typed.trim();
```

- **Files reviewed:** 25/25 changed files
- **Comments generated:** 1
- **Review effort level:** Lite

</details>

---

💡 <a href="/TonyHopland/Ild/new/main?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="https://docs.github.com/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review?tool=webui#mcp-servers-and-agent-skills" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>
